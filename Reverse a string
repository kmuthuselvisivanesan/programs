// Program to reverse the string

package programs;

public class String_Reverse {

	public static void main(String[] args) {
		StringBuilder a = new StringBuilder("My name is Muthuselvi");
		System.out.println(a);
		StringBuilder b=new StringBuilder();
		for(int i=a.length()-1;i>=0;i--) // to access the elements from right...reverse and gets stored in b
		    {
			b.append(a.charAt(i));
		    }
		System.out.println(b);
		
			
		}
		

	}




output:

My name is Muthuselvi
ivlesuhtuM si eman yM
